# opdracht-switching-routing

Voorlopige build command voor sensors: ```docker build -t sensors .``` (in terminal in sensors folder).

## web-ui
node-red: ``localhost:1880``
influxdb: ``localhost:8086``